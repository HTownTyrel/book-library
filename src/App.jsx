import ReadingTracker from "./ReadingTracker.jsx";

// App is the top-level component Vite renders into the page.
// For now, all it does is render your ReadingTracker component.
function App() {
  return <ReadingTracker />;
}

export default App;
